{
  "name": "claude-squad-tests",
  "private": true,
  "type": "module",
  "description": "Structural tests for the squad Claude Code plugin.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "yaml": "^2.5.0"
  }
}
